<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Masquer les modules standards pour tous sauf les admins -->

    <!-- Applications principales -->
    <record id="base.menuv2_custom" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer CRM -->
    <record id="crm.crmv2_menu_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Ventes -->
    <record id="sale.salev2_menu_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Comptabilité -->
    <record id="account.menuv2_finance" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Site Web -->
    <record id="website.menuv2_website_configuration" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Ressources humaines -->
    <record id="hr.menuv2_hr_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Applications sociales / Discuss -->
    <record id="mail.mailv2_channel_menu_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Point de vente -->
    <record id="point_of_sale.menuv2_point_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Achats -->
    <record id="purchase.purchasev2_menu_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>

    <!-- Masquer Inventaire standard Odoo -->
    <record id="stock.menuv2_stock_root" model="ir.ui.menu">
        <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
    </record>
</odoo>

