<odoo>
    <data noupdate="0">
        <!-- Record Rule: Users only see apps if they belong to one of the Allowed Groups -->
        <!-- Logic: If group_ids is Empty (Global) OR User is in one of group_ids -->
        <record id="rule_business_app_visibility" model="ir.rule">
            <field name="name">Business App: User Visibility</field>
            <field name="model_id" ref="model_business_app"/>
            <field name="domain_force">['|', ('group_ids', '=', False), ('group_ids', 'in', user.groups_id.ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
