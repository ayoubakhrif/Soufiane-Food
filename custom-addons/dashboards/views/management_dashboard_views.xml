<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- FORM VIEW -->
    <record id="view_management_dashboard_form" model="ir.ui.view">
        <field name="name">management.dashboard.form</field>
        <field name="model">management.dashboard</field>
        <field name="arch" type="xml">
            <form string="Management Dashboard" create="false" edit="false" delete="false">
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <field name="content_html" widget="html" readonly="1" options="{'style-inline': true}"/>
                </sheet>
            </form>
        </field>
    </record>

    <!-- KANBAN VIEW -->
    <record id="view_management_dashboard_kanban" model="ir.ui.view">
        <field name="name">management.dashboard.kanban</field>
        <field name="model">management.dashboard</field>
        <field name="arch" type="xml">
            <kanban create="false" class="o_kanban_dashboard">
                <field name="name"/>
                <field name="dashboard_type"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                                <div class="text-muted">
                                    <field name="dashboard_type"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_management_dashboard" model="ir.actions.act_window">
        <field name="name">Tableaux de Bord</field>
        <field name="res_model">management.dashboard</field>
        <field name="view_mode">kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No dashboards available.
            </p>
        </field>
    </record>

    <!-- ROOT MENU (Moved here since legacy file was deleted) -->
    <menuitem id="menu_dashboards_root"
              name="Dashboards"
              web_icon="dashboards,static/description/icon.png"
              sequence="1"/>

    <!-- MENU -->
    <!-- ACTIONS FOR SPECIFIC DASHBOARDS -->
    <record id="action_dashboard_profit_client" model="ir.actions.act_window">
        <field name="name">Bénéfice par Client</field>
        <field name="res_model">management.dashboard</field>
        <field name="view_mode">form</field>
        <field name="res_id" ref="dashboards.dashboard_profit_client"/>
    </record>

    <record id="action_dashboard_profit_product" model="ir.actions.act_window">
        <field name="name">Bénéfice par Produit</field>
        <field name="res_model">management.dashboard</field>
        <field name="view_mode">form</field>
        <field name="res_id" ref="dashboards.dashboard_profit_product"/>
    </record>

    <!-- MENU STRUCTURE -->
    <menuitem id="menu_benefit_control_root"
              name="Benefit Control"
              parent="menu_dashboards_root"
              sequence="10"/>

    <menuitem id="menu_dashboard_profit_client"
              name="Profit par Client"
              parent="menu_benefit_control_root"
              action="action_dashboard_profit_client"
              sequence="1"/>

    <menuitem id="menu_dashboard_profit_product"
              name="Profit par Produit"
              parent="menu_benefit_control_root"
              action="action_dashboard_profit_product"
              sequence="2"/>

</odoo>
