<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================= -->
    <!-- 📋 Form View -->
    <!-- ========================= -->
    <record id="view_expense_category_form" model="ir.ui.view">
        <field name="name">suivi.expense.category.form</field>
        <field name="model">suivi.expense.category</field>
        <field name="arch" type="xml">
            <form string="Catégorie de Dépense">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                   options="{'terminology': 'archive'}"/>
                        </button>
                    </div>
                    <group string="Suivi du mois" col="4">
                        <div class="oe_stat_box" style="margin-top:10px;">

                            <!-- Limite mensuelle -->
                            <div class="oe_stat_button">
                                <span class="o_stat_value">
                                    <field name="monthly_limit" nolabel="1"/>
                                    <field name="is_daily" widget="boolean_toggle"
                                        options="{'terminology': 'Daily / Monthly'}"/>
                                </span>
                                <span class="o_stat_text">Monthly Limit</span>
                            </div>

                            <!-- Situation actuelle -->
                            <div class="oe_stat_button"
                                attrs="{'class': [('limit_exceeded','=',True,'text-danger'),
                                                ('limit_exceeded','=',False,'text-success')]}">
                                <span class="o_stat_value">
                                    <field name="current_balance" nolabel="1"/>
                                </span>
                                <span class="o_stat_text">Current Situation</span>
                            </div>

                            <!-- Statut -->
                            <div class="oe_stat_button">
                                <span class="o_stat_value">
                                    <field name="limit_exceeded"
                                        widget="boolean"
                                        options="{'terminology': 'exceeded'}"
                                        nolabel="1"/>
                                </span>
                                <span class="o_stat_text">Status</span>
                            </div>

                        </div>
                    </group>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Nom de la catégorie..."/>
                        </h1>
                    </div>
                    <group>
                        <field name="description" placeholder="Description optionnelle..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ========================= -->
    <!-- 🌳 Tree View -->
    <!-- ========================= -->
    <record id="view_expense_category_tree" model="ir.ui.view">
        <field name="name">suivi.expense.category.tree</field>
        <field name="model">suivi.expense.category</field>
        <field name="arch" type="xml">
            <tree string="Catégories de Dépenses">
                <field name="name"/>
                <field name="description"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <!-- ========================= -->
    <!-- 🚀 Action -->
    <!-- ========================= -->
    <record id="action_expense_category" model="ir.actions.act_window">
        <field name="name">Catégories de Dépenses</field>
        <field name="res_model">suivi.expense.category</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
