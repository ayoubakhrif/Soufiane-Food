<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================= -->
    <!-- 📋 Form View -->
    <!-- ========================= -->
    <record id="view_expense_monthly_form" model="ir.ui.view">
        <field name="name">suivi.expense.monthly.form</field>
        <field name="model">suivi.expense.monthly</field>
        <field name="arch" type="xml">
            <form string="Dépense Mensuelle">
                <sheet>
                    <group>
                        <group>
                            <field name="category_id"/>
                            <field name="amount"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Description optionnelle..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ========================= -->
    <!-- 🌳 Tree View (Editable) -->
    <!-- ========================= -->
    <record id="view_expense_monthly_tree" model="ir.ui.view">
        <field name="name">suivi.expense.monthly.tree</field>
        <field name="model">suivi.expense.monthly</field>
        <field name="arch" type="xml">
            <tree string="Dépenses Mensuelles" editable="bottom">
                <field name="category_id"/>
                <field name="amount" sum="Total"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <!-- ========================= -->
    <!-- 🚀 Action -->
    <!-- ========================= -->
    <record id="action_expense_monthly" model="ir.actions.act_window">
        <field name="name">Dépenses Mensuelles</field>
        <field name="res_model">suivi.expense.monthly</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
